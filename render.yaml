services:
  - type: web
    name: reversal-api
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    autoDeploy: true

    envVars:
      - key: NODE_VERSION
        value: 22